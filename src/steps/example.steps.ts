// Example step definitions for Cucumber/Gherkin
import { Given, When, Then } from '@cucumber/cucumber';

Given('I am on the homepage', async () => {
  // Step implementation
});

When('I click the login button', async () => {
  // Step implementation
});

Then('I should see the dashboard', async () => {
  // Step implementation
});
