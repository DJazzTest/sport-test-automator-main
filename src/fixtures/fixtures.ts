// Global fixtures for Playwright tests
// Add reusable setup, teardown, and mock data here

export const baseUrl = process.env.BASE_URL || 'https://example.com';

export function setupEnvironment() {
  // Setup logic before tests run
}

export function teardownEnvironment() {
  // Cleanup logic after tests
}
